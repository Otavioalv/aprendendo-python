set up vs code X
	pluguiujn e configurações X	

git X
	add X
	commit X
	push X
	


gihub X
	logar e aprender X

python ver oq sabem sobre X
	instalar python X
	aprender oq e biblioteca X
	oq e framework  X
	ambiente virtual venv X
	saber oq e o venv X	 	
		python -m venv <nome do ambviente> // geralmente o nome e my-venvX
	iniciar o venv X
		nome do ambiente\scripts\activate X
	instalar o flask  X
		pip install Flask X  
	aprender oq e o arquivo requirements.txt X
		pip freeze > requirements.txt x
	aprender a colocar as dependencias no arquivo requirements.txt X
	intalar dependencias atraves do arquivo requirements.txt  X
 		pip install -r requirements.txt X

postman X
	instalar X
	testar a api com postman X

flask aprender	
	Pouco de orientação a objetos X
	metodos http
		get solicita dados, 
		post Submete dados para serem processados para um recurso especificado., 
		delete deleta...,
		put  Substitui todos os atuais,
		patch	Aplica modificações parciais a um recurso.
	end-point X
	pegar dados pelo end point X 	
		/<dado> X
		na funcao (dado) X
	pegar dados atraves de json/xml etc
		request.get_json();
	enviar dados por json/xml etc
	codigo de status http basico
		200 - success/ok
		404 - erro
		500 - erro interno serviv cdor
		400 - ma requisição 

